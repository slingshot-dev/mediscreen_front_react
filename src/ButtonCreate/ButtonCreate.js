/*const ButtonCreate = ({idPatient}) => {
    const onClick = () => {
        fetch(`/api/patients/list?id=${idPatient}`, {
            method: 'GET'
        }).then(response => {
            console.log(response.json());
        });
    }
    // return <button onClick={onClick}>Delete {idPatient}</button>;
    return <button type="button" className="btn btn-primary mr-2 mb-2" onClick={onClick}>Create</button>
}

export default ButtonCreate;*/
